/** Shared OnePage CTA button/link chrome for project cards. */
export const actionLinkClass =
  'inline-flex h-9 shrink-0 items-center justify-center gap-1.5 rounded-lg border border-[var(--border-subtle)] px-2.5 text-xs font-medium hover:border-brand/50';

/** How many times the tech marquee track is repeated for a seamless loop. */
export const STACK_MARQUEE_REPEATS = 8;
