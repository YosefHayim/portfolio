# Frontend Environment Variables
# Not needed in production — the worker serves both API and frontend from the same origin.
# Only used in local dev when running vite separately from wrangler.
VITE_API_URL=http://localhost:3004
